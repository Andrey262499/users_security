# Тестовые учетки

* `admin@mail.ru` / `admin` (ROLE_ADMIN)
* `user@mail.ru` / `user` (ROLE_USER)

# Страницы

* `/login` форма логина
* `/admin` админка (CRUD)
* `/user` страница текущего пользователя
